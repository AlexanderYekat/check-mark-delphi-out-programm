# Проверка марок на ККТ через CSV файлы

## 📋 Описание

Упрощенная система обмена данными между 1С и Delphi программой через CSV файлы.

## 📁 Структура файлов

### Входные файлы (создает 1С):

1. **`c:\share\input_marks.csv`** - марки для проверки
   ```csv
   Position;MarkCode;PlannedStatus
   1;014494550435306821QXYXSALGLMYQQ\u001d91EE06;штучный товар, реализован
   2;01047510084300422152yWbQ\u001d930Hw+;штучный товар, реализован
   ```

2. **`c:\share\input_params.csv`** - параметры чека
   ```csv
   CheckType;CashierName;TimeZone
   sell;Иванов Иван Иванович;6
   ```

### Выходной файл (создает Delphi):

3. **`c:\share\output_results.csv`** - результаты проверки
   ```csv
   Position;MarkCode;KKTCheckCode;KKTCheckDescription;imcCheckFlag;imcCheckResult;imcStatusInfo;imcEstimatedStatusCorrect;PermitCheckCode;PermitCheckDescription;UUID;TimeStamp;Inst;Ver
   1;014494...;0;OK;1;1;1;1;0;;;;;;
   ```

## 🔧 Функции 1С

### 1. ЗаписатьМаркиДляПроверки()
Записывает марки и параметры в CSV файлы.

**Параметры:**
- `МассивМарок` - массив структур с полями:
  - `Позиция` (Число)
  - `КодМаркировки` (Строка)
  - `ПланируемыйСтатус` (Строка)
- `ПараметрыЧека` - структура с полями:
  - `ТипЧека` (Строка): "sell" или "return"
  - `ИмяКассира` (Строка)
  - `ТаймЗона` (Число): 1-12, по умолчанию 6

**Пример:**
```bsl
МассивМарок = Новый Массив;

Марка = Новый Структура;
Марка.Вставить("Позиция", 1);
// Кодируем марку в Base64
МаркаОбычная = "014494550435306821QXYXSALGLMYQQ" + Символ(29) + "91EE06";
Марка.Вставить("КодМаркировкиBase64", ЗакодироватьМаркуВBase64(МаркаОбычная));
Марка.Вставить("ПланируемыйСтатус", "штучный товар, реализован");
МассивМарок.Добавить(Марка);

ПараметрыЧека = Новый Структура;
ПараметрыЧека.Вставить("ТипЧека", "sell");
ПараметрыЧека.Вставить("ИмяКассира", "Иванов И.И.");
ПараметрыЧека.Вставить("ТаймЗона", 6);

ЗаписатьМаркиДляПроверки(МассивМарок, ПараметрыЧека);
```

### 2. ПрочитатьРезультатыПроверки()
Читает результаты из CSV файла.

**Возвращает:** Массив структур с полями:
- `Позиция`
- `КодМаркировки`
- `КодПроверкиККТ` (0 = успех)
- `ОписаниеРезультатаККТ`
- `imcCheckFlag` (Булево)
- `imcCheckResult` (Булево)
- `imcStatusInfo` (Булево)
- `imcEstimatedStatusCorrect` (Булево)
- И другие поля...

**Пример:**
```bsl
Результаты = ПрочитатьРезультатыПроверки();

Для Каждого Рез Из Результаты Цикл
    Если Рез.КодПроверкиККТ = 0 И Рез.imcCheckResult Тогда
        Сообщить("✓ Марка валидна");
    Иначе
        Сообщить("✗ Марка невалидна");
    КонецЕсли;
КонецЦикла;
```

### 3. ОчиститьCSVФайлы()
Удаляет все CSV файлы после обработки.

## 🎯 Процесс работы

### Вариант 1: Ручной режим

1. **В 1С:** Подготовить данные и записать CSV
   ```bsl
   ТестПроверкиМарокЧерезCSV();
   ```

2. **В Delphi:**
   - Запустить программу
   - Нажать "Загрузить данные" (ButtonCreateNewSession)
   - Проверить марки на ККТ
   - Нажать "Сохранить результаты" (ButtonFinishSession)

3. **В 1С:** Прочитать результаты
   ```bsl
   ПрочитатьИОбработатьРезультаты();
   ```

### Вариант 2: Быстрая проверка

```bsl
// Проверить одну марку
ПроверитьОднуМарку("014494550435306821...", "Иванов И.И.");

// Затем в Delphi обработать
// Затем в 1С прочитать результат
```

## 📝 Примеры использования

### Пример 1: Проверка нескольких марок

```bsl
МассивМарок = Новый Массив;

// Марка 1
Марка1 = Новый Структура;
Марка1.Вставить("Позиция", 1);
Марка1.Вставить("КодМаркировки", "014494..." + Символ(29) + "91EE06");
Марка1.Вставить("ПланируемыйСтатус", "штучный товар, реализован");
МассивМарок.Добавить(Марка1);

// Марка 2
Марка2 = Новый Структура;
Марка2.Вставить("Позиция", 2);
Марка2.Вставить("КодМаркировки", "01047..." + Символ(29) + "930Hw+");
Марка2.Вставить("ПланируемыйСтатус", "штучный товар, реализован");
МассивМарок.Добавить(Марка2);

ПараметрыЧека = Новый Структура;
ПараметрыЧека.Вставить("ТипЧека", "sell");
ПараметрыЧека.Вставить("ИмяКассира", "Петров П.П.");
ПараметрыЧека.Вставить("ТаймЗона", 6);

Если ЗаписатьМаркиДляПроверки(МассивМарок, ПараметрыЧека) Тогда
    Сообщить("CSV файлы готовы для обработки");
КонецЕсли;
```

### Пример 2: Обработка результатов

```bsl
Результаты = ПрочитатьРезультатыПроверки();

ВалидныеМарки = 0;
НевалидныеМарки = 0;

Для Каждого Рез Из Результаты Цикл
    Если Рез.КодПроверкиККТ = 0 И Рез.imcCheckResult Тогда
        ВалидныеМарки = ВалидныеМарки + 1;
    Иначе
        НевалидныеМарки = НевалидныеМарки + 1;
        Сообщить("Невалидная марка: " + Рез.КодМаркировки);
    КонецЕсли;
КонецЦикла;

Сообщить("Валидных: " + ВалидныеМарки);
Сообщить("Невалидных: " + НевалидныеМарки);
```

## ⚙️ Настройка

### Планируемые статусы марок:
- `"штучный товар, реализован"` - при продаже штучного товара
- `"штучный товар, возвращен"` - при возврате штучного товара
- `"мерный товар, реализован"` - при продаже мерного товара
- `"мерный товар, в стадии реализации"` - промежуточный статус

### Типы чеков:
- `"sell"` - продажа
- `"return"` - возврат

### Часовые пояса:
- 1-12 (по умолчанию 6 - МСК+3)

## 🐛 Отладка

### Проверка создания файлов:
```bsl
Файл = Новый Файл("c:\share\input_marks.csv");
Если Файл.Существует() Тогда
    Сообщить("✓ Файл марок создан");
Иначе
    Сообщить("✗ Файл марок не создан");
КонецЕсли;
```

### Проверка результатов:
```bsl
Файл = Новый Файл("c:\share\output_results.csv");
Если Файл.Существует() Тогда
    Сообщить("✓ Результаты готовы");
    ПрочитатьРезультатыПроверки();
Иначе
    Сообщить("⏱ Ждем обработки в Delphi");
КонецЕсли;
```

## 📦 Файлы модуля

- **ПроверкаМарокЧерезCSV.bsl** - основные функции
- **ПримерИспользованияCSV.bsl** - примеры и тесты
- **1ctest_CSV.bsl** - тестовый модуль

## ✅ Преимущества

- ✅ Простая структура CSV
- ✅ Легко читать и отлаживать
- ✅ Не нужны сложные JSON парсеры
- ✅ Файлы можно открыть в Excel
- ✅ Понятная схема обмена данными

## 🔄 Версия

Версия: 1.0  
Дата: 2025-01-10

