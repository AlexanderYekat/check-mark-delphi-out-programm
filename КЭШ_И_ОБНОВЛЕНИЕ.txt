═══════════════════════════════════════════════════════════
  КЭШ РЕЗУЛЬТАТОВ: Добавление и Обновление
═══════════════════════════════════════════════════════════

🔄 ЛОГИКА РАБОТЫ С КЭШЕМ:
--------------------------

AddMarkToCache(markResult) - Умная функция!

1️⃣ ПОИСК В КЭШЕ:
   • Ищет марку по MarkCodeBase64
   • Перебирает весь массив FCheckCache

2️⃣ ЕСЛИ МАРКА НАЙДЕНА:
   • ОБНОВЛЯЕТ существующую запись
   • FCheckCache[i] := MarkResult
   • Логирует: "Результат обновлен в кэше"
   
3️⃣ ЕСЛИ МАРКА НЕ НАЙДЕНА:
   • ДОБАВЛЯЕТ новую запись
   • SetLength(FCheckCache, +1)
   • Логирует: "Результат добавлен в кэш"


💡 ЗАЧЕМ ЭТО НУЖНО?
--------------------

✅ Можно ПЕРЕПРОВЕРИТЬ марку
   • Первая проверка неудачна
   • Исправляем параметры
   • Проверяем снова
   • Результат ОБНОВИТСЯ в кэше

✅ НЕТ дубликатов в кэше
   • Одна марка = одна запись
   • Всегда актуальный результат

✅ Сохраняется последний результат
   • Если марку проверяли 5 раз
   • В кэше - результат последней проверки


📊 ПРИМЕРЫ:
-----------

СЦЕНАРИЙ 1: Первая проверка марки
----------------------------------
1. Марка "01234..." → проверка на ККТ
2. Результат: errorCode=0, ValidationResult=15
3. ButtonAddToTable → AddMarkToCache()
   → Марки нет в кэше
   → ДОБАВЛЯЕМ новую запись
   → Размер кэша: 1

СЦЕНАРИЙ 2: Повторная проверка (обновление)
---------------------------------------------
1. Та же марка "01234..." → снова проверка
2. Новый результат: errorCode=5, ValidationResult=10
3. ButtonAddToTable → AddMarkToCache()
   → Марка УЖЕ ЕСТЬ в кэше
   → ОБНОВЛЯЕМ запись
   → Размер кэша: 1 (не увеличился!)

СЦЕНАРИЙ 3: Проверка разных марок
-----------------------------------
1. Марка "01234..." → результат в кэш
   Размер: 1
2. Марка "05678..." → результат в кэш
   Размер: 2
3. Снова "01234..." → обновление
   Размер: 2 (не изменился)


🔍 КАК ЭТО ВИДНО В ЛОГАХ:
--------------------------

Первое добавление:
[14:10:15] Результат добавлен в кэш для марки на позиции 1
[14:10:15] Размер кэша: 1 марок

Обновление:
[14:12:30] Результат обновлен в кэше для марки на позиции 1
[14:12:30] Размер кэша: 1 марок

Новая марка:
[14:15:45] Результат добавлен в кэш для марки на позиции 2
[14:15:45] Размер кэша: 2 марок


🎯 КЛЮЧЕВАЯ ЛОГИКА:
--------------------

```pascal
// Ищем марку в кэше
for i := 0 to High(FCheckCache) do
begin
  if FCheckCache[i].MarkCodeBase64 = MarkResult.MarkCodeBase64 then
  begin
    // НАШЛИ - ОБНОВЛЯЕМ
    FCheckCache[i] := MarkResult;
    Exit;
  end;
end;

// НЕ НАШЛИ - ДОБАВЛЯЕМ
SetLength(FCheckCache, Length(FCheckCache) + 1);
FCheckCache[High(FCheckCache)] := MarkResult;
```


💾 ЧТО СОХРАНЯЕТСЯ В CSV:
--------------------------

Только ПОСЛЕДНИЙ результат для каждой марки!

Пример:
• Марка "01234..." проверялась 3 раза
• В кэше - результат 3-й проверки
• В CSV сохранится - результат 3-й проверки


✅ ПРЕИМУЩЕСТВА:
-----------------

✅ Нет дубликатов в кэше
✅ Всегда актуальные данные
✅ Можно исправить ошибки повторной проверкой
✅ Размер кэша = уникальные марки
✅ Простая логика обновления


🔧 ИСПОЛЬЗОВАНИЕ:
------------------

Проверили марку:
  → ButtonAddToTable
    → AddMarkToCache()
      → Автоматически добавит ИЛИ обновит

Проверили повторно:
  → ButtonAddToTable
    → AddMarkToCache()
      → Автоматически ОБНОВИТ существующую

Все работает автоматически! 🚀


═══════════════════════════════════════════════════════════
  Версия: 2.0 | Дата: 2025-01-10
═══════════════════════════════════════════════════════════

